npm create vite@latest my-app -- --template vanilla
cd my-app
npm install
npm run dev
npm run build

{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
